<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Transaction PDF</title>
  <style>
    /* Include Tailwind CSS styles */
    <%= wicked_pdf_stylesheet_link_tag('https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css') %>
  </style>
</head>
<body class="p-8">
  <h1 class="text-2xl mb-6">Transaction Details</h1>
  <table class="w-full">
    <thead>
      <tr>
        <th class="border-b border-gray-300">Amount</th>
        <th class="border-b border-gray-300">Contact Name</th>
        <th class="border-b border-gray-300">Description</th>
        <th class="border-b border-gray-300">Transaction Date</th>
      </tr>
    </thead>
    <tbody>
      <% @transactions.each do |transaction| %>
        <tr>
          <td class="border-b border-gray-300 p-2"><%= transaction.amount %></td>
          <td class="border-b border-gray-300 p-2"><%= transaction.contact_name %></td>
          <td class="border-b border-gray-300 p-2"><%= transaction.description %></td>
          <td class="border-b border-gray-300 p-2"><%= transaction.transaction_at %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</body>
</html>